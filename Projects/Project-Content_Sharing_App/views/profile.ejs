<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class="w-full flex gap-5 justify-end">
            <a class="px-3 py-2 text-sm inline-block mb-5 bg-yellow-500 text-black rounded-md" href="/profile/upload">Edit Profile</a>
            <a class="px-3 py-2 text-sm inline-block mb-5 bg-red-500 rounded-md"href="/logout">Logout</a>
        </div>
        <h3 class="text-4xl mb-8">Profile</h3>
        <div class="flex items-start gap-3 mb-3">
            <div class="w-10 h-10 rounded-md overflow-hidden">
                <img class=" w-full h-full object-cover"src="images/uploads/<%= user.profilepic%>" alt=""> 
            </div>
            <h3 class="text-3xl"><span class="font-light">Hello</span>, <%= user.username%></h3>
        </div>
         
        <h5 class="mb-5">You can create a new post.</h5>
        <form action="/post" method="post">
            <textarea name="content" placeholder="Whats on your mind ?"class="w-1/3 p-3 outline-none resize-none bg-transparent border-2 border-zinc-800 rounded-md"></textarea>
            <input type="submit" value="Create New Post" class="px-3 text-sm py-2 w-40 bg-blue-500 block rounded-md mt-2">
        </form>
        <div class="posts mt-20">
            <h3 class="text-zinc-400">Your Posts.</h3>
            <div class="postcontainer mt-5">

                <% user.posts.reverse().forEach(function(post){ %>       
                    <div class="post mb-4 w-1/3 p-4 border-[1px] border-zinc-700 mt-5 rounded-md bg-zinc-800">
                        <h4 class="text-blue-500 mb-2">@<%= user.username %></h4>
                        <p class="text-sm tracking-tight"><%= post.content %></p>
                        <small class="mt-4 inline-block"><%= post.likes.length%> likes</small>
                        <div class="btns flex mt-1 gap-4">
                            <a class="text-blue-500" href="/like/<%=post._id %>">
                                <%= post.likes.indexOf(user._id)=== -1 ? "Like" : "Unlike" %>
                            </a>
                            <a href="/edit/<%= post._id%> "class="text-zinc-600">Edit</a>
                            <a class="bg-red-500 px-1 py-0.5 text-sm tracking-tighter rounded-md cursor-pointer" href="/delete/<%= post._id%>">Delete</a>
                        </div>
                    </div> 
    
                <% })%>
            </div>
        </div>
    </div>
</body>
</html>